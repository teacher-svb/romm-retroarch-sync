# Core dependencies for RomM-RetroArch Sync
#
# The sync engine now lives in the Ludo repo and is shared by both frontends.
# It pulls in requests / watchdog / cryptography / Pillow / psutil / py7zr
# itself, so they are no longer listed here.
#
# Pinned to a specific commit: bumping it is an explicit, reviewable change.
romm-sync-engine @ git+https://github.com/Covin90/ludo@354fa67#subdirectory=engine

# For local development, prefer an editable install pointed at your checkout so
# engine edits are picked up by both apps immediately, with no reinstall:
#   pip install -e ~/ludo/engine

# GTK4 and GObject bindings (system-provided, listed for reference)
# PyGObject>=3.42.0

# Optional: Development and testing dependencies
# pytest>=7.0.0
# flake8>=4.0.0
